apiVersion: v1
kind: Service
metadata:
  name: customer-support-api
  namespace: customer-support
  labels:
    app: customer-support-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: customer-support-api
